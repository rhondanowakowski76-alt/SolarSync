This file only exists so the "data" folder exists in the repo.
The app's built-in database (PGlite) writes to data/pg and needs this
folder to be present, otherwise startup crashes with:
  ENOENT: no such file or directory, mkdir '.../data/pg'

On GitHub, this file should be named:  data/.gitkeep
(i.e. inside a folder called "data").
